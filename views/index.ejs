<h1><%= title %></h1>

<% if (user) { %>

<h2>post something</h2>
<form method="POST" action="/post">
  <div>
    <input id="post" placeholder="post" name="post" type="text" />
  </div>
</form>

<% } %>

<ul>
  <% if (post_list) for (post of post_list) { %>
  <li>
    <b><%= user ? post.sender : "???" %></b>
    <p><%= post.content %></p>
    <p>
      — <%= user ? new Date(post.timeSent).toLocaleString(undefined, {year:
      "numeric", month: "numeric", day: "numeric", hour: "numeric", minute:
      "numeric"}) : "???"%>
    </p>
  </li>
  <% if (user?.admin === true) { %>
  <form method="POST" action="/delete/:id">
    <input
      type="hidden"
      id="hidden"
      name="postId"
      value="<%= `${post._id}` %>"
    />
    <button id="submit" type="submit">delete</button>
  </form>

  <% } %> <% } else { %>
  <li>There are no posts.</li>
  <% } %>
</ul>
